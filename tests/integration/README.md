# Integration Tests

Integration tests will be added when Supabase is configured.

Planned tests:
- Full checkout flow (create order → verify DB state)
- Payment webhook → order status update
- Download token generation after payment
- Download token validation and file delivery
